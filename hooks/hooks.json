{
	"hooks": {
		"PreToolUse": [
			{
				"matcher": "Bash",
				"hooks": [
					{
						"type": "command",
						"command": "$(command -v cc-safety-net || echo 'npx -y cc-safety-net') --claude-code"
					}
				]
			}
		]
	}
}
